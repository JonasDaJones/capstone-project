import styled from "styled-components";

export const StyledBypassInputContainer = styled.div`
  display: flex;
  align-items: center;
  border: 1px solid var(--light);
  margin-bottom: 0.7em;
`;

export const StyledBypassInputSelect = styled.select`
  margin-right: 1em;
  text-align: center;
  background: transparent;
  padding: 0.2em;
  color: var(--brightfont);
`;

export const StyledBypassLabel = styled.label`
  display: none;
`;
